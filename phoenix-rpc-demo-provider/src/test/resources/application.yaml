server:
  port: 8084

phoenix:
  rpc:
    registry:
      zookeeper:
        server: localhost:2182
        root: phoenix-rpc

app:
  id: app1
  namespace: public
  env: dev

logging:
  level:
    root: error
    cn.zyszero.phoenix.rpc: debug