## HA
1. 有节点宕机的时候，通过多个 provider 集群 + 注册中心，可以运行起保障服务整体的可用。
2. 有节点偶尔异常，但没有宕机，我们可以通过重试 + LB 重新选节点，实现这次调用的成功。
3. 有节点在一段时间内异常（这个实例上有很多服务，其中个别服务，比如一个复杂的服务，跑了许多 SQL，其中某个 SQL 在 DB 执行非常缓慢），节点未宕机，节点上的其他服务一直是正常的 ==》 故障隔离
   - 探活好了，就可以做故障恢复，full open
   - 每次定时探活，我们放一笔流量进来，half open